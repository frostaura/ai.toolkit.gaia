namespace Gaia.Mcp.Server.Tools;

// Placeholder: implement project-scoped stable memory storage.
public sealed class MemoryTool
{
    // Store stable facts only (how to run, env vars, conventions).
}
