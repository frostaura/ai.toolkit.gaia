# SKILL: Stack Default — .NET MAUI

## Baseline
- build: `dotnet build`
- lint/format: editorconfig + analyzers / `dotnet format` when available
- tests: unit tests + platform-appropriate integration where feasible
- CI: build + tests + lint checks
